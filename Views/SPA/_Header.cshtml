<a class="brand" href="#">ngStates</a>
<ul class="nav">
    <li ng-class="{ active: $state.includes('contacts') }" ng-repeat="link in links"><a href="#{{link.url}}">{{link.name}}</a></li>
</ul>
<p class="navbar-text pull-right" ui-view="hint"></p>
